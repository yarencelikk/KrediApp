<Shell
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:pages="clr-namespace:KrediApp"
    x:Class="KrediApp.AppShell">

    <FlyoutItem Title="Ana Sayfa" Icon="home.png">
        <ShellContent ContentTemplate="{DataTemplate pages:MainPage}" />
    </FlyoutItem>

    <FlyoutItem Title="Kredi Hesaplama" Icon="calculator.png">
        <ShellContent ContentTemplate="{DataTemplate pages:KrediHesaplamaPage}" />
    </FlyoutItem>

    <FlyoutItem Title="Vücut Kitle İndeksi" Icon="vki.png">
        <ShellContent ContentTemplate="{DataTemplate pages:VkiHesaplamaPage}" />
    </FlyoutItem>

    <FlyoutItem Title="Renk Seçici" Icon="colorpicker.png">
        <ShellContent ContentTemplate="{DataTemplate pages:RenkSeciciPage}" />
    </FlyoutItem>


</Shell>
